import XCTest
import SwiftUI
@testable import TopScrollingTabBar

final class TopScrollingTabBarPickerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(TopScrollingTabBar(selection: .constant(PreviewTab.archived), content: { tab in
//            Text("Hello World")
//        }).self, TopScrollingTabBar<PreviewTab, Text>.Type)
    }
}
